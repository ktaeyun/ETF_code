# 결정 사항과 그 결과

> 한국형 비트코인 ETF 가격 시뮬레이션 연구
> 정리 기준일: 2026-08-30 / 기준 커밋: `cbb7b22`

이 문서는 **무엇을 결정했고, 그 결정이 실제로 어떤 결과를 냈는지**를 정리한 것이다.
배경 지식이 없어도 읽을 수 있도록 용어와 판단 근거를 함께 풀어 썼다.

---

## 0. 이 연구가 하려는 것

한국에는 비트코인 현물 ETF가 아직 없다. 만약 상장된다면 가격이 어떻게 움직일지,
특히 **시장이 불안할 때 얼마나 위험할지**를 시뮬레이션으로 추정하는 것이 목표다.

가격을 한 덩어리로 보지 않고 **세 조각의 곱**으로 나눈다.

```
한국 ETF 가격 = NAV × (1 + GAP) × (1 + KP)
```

| 조각 | 뜻 | 쉽게 말하면 |
|---|---|---|
| **NAV** | 기초자산 순자산가치 | 비트코인 자체의 가치 |
| **GAP** | ETF 괴리율 | ETF 가격이 NAV보다 몇 % 비싸게/싸게 거래되는가 |
| **KP** | 김치프리미엄 | 한국 거래소 비트코인이 해외보다 몇 % 비싼가 |

세 조각은 성질이 완전히 다르다. NAV는 계속 오르내리며 방향을 예측하기 어렵고(랜덤워크에 가깝다),
GAP과 KP는 벌어지면 다시 좁혀지는 성질(평균회귀)이 있다. 그래서 **각각 다른 수학 모형**을 쓰고,
마지막에 곱해서 합친다.

전체 절차는 11단계다.

- **Phase 1 (Step 1~7)**: 데이터 수집 → 변수 선별 → 모형 설계 → 검증 → 결합 → 통합 검증 → 가격 경로 생성
- **Phase 2 (Step 8~11)**: 시나리오 설계 → 외생변수 재생성 → 시나리오별 시뮬레이션 → 기준(Base) 대비 비교

---

## 1. 결정 요약표

이번 작업에서 내린 결정과 그 결과를 한눈에 정리하면 다음과 같다.

| # | 결정 | 왜 | 결과 |
|---|---|---|---|
| 1 | 자체 지표 WMCR을 버리고 **예측구간 커버리지**로 교체 | WMCR의 계산식에 구조적 결함 | GAP "미통과"가 허위 경보였음이 드러남 |
| 2 | 커버리지 판정을 **모형 내재 기준분포**로 | 날값을 명목값과 비교하면 오독함 | "NAV 과대분산" 오판을 잡아냄 |
| 3 | Step 5·6을 **3자 결합 검증**으로 수정 | 기존 검정이 대수적으로 무의미했음 | 통합 검증이 실제 검증이 됨 |
| 4 | 보조지표 10종 **삭제** | 프레임워크가 3개 검정만 규정 | 미결 항목 4건 동시 소멸 |
| 5 | 모형 비교 코드 **전부 삭제** | 모형은 선행연구+데이터 특성으로 선정 | 코드 4,511줄 제거 |
| 6 | GAP에 **Student-t 혁신항** 도입 | 실제 괴리율의 꼬리가 두꺼움 | GAP 이상 판정 완전 해소 |
| 7 | KP를 **3레짐 → 2레짐**으로 축소 | 세 번째 레짐 점유율이 0% | 추정 안 된 모수 제거, 결과 불변 |

아래에서 각각을 자세히 다룬다.

---

## 2. 결정 1 — 검증 지표를 WMCR에서 예측구간 커버리지로

### 2.1 무엇이 문제였나

시뮬레이션이 잘 되었는지 판단하려면 "모형이 만든 1,000개 경로가 실제 경로를 잘 감싸는가"를
봐야 한다. 기존에는 이를 **WMCR**이라는 자체 고안 지표로 쟀다.

WMCR은 밴드(허용 범위)를 이렇게 정의했다.

```
밴드 중심 = (1,000개 경로의 최솟값 + 최댓값) / 2
밴드 범위 = 중심 ± p%
```

**여기에 결함이 있다.** 최솟값과 최댓값은 표본 개수가 늘어날수록 계속 벌어지는 통계량이다.
경로를 1,000개 뽑든 10,000개 뽑든 모형은 그대로인데, 경로를 많이 뽑을수록 밴드가 넓어진다.
즉 **밴드 폭이 모형의 성질이 아니라 시뮬레이션 횟수에 끌려간다.**

### 2.2 무엇으로 바꿨나

**예측구간 커버리지(PICP)** 로 교체했다. 원리는 단순하다.

1. 각 시점에서 1,000개 경로의 **분위수**로 구간을 만든다 (예: 하위 2.5% ~ 상위 97.5% = 95% 구간)
2. 실제 값이 그 구간 안에 들어간 날의 비율을 센다
3. 그 비율이 명목수준(95%)에 가까울수록 잘 맞는 모형이다

분위수는 표본이 늘어도 안정적이라 WMCR의 문제가 없다. 그리고 예측 분야의 표준 관행이라
논문에서 따로 정당화할 필요도 없다.

구간을 넓게 잡으면 커버리지는 저절로 올라가므로, **구간 폭**도 함께 잰다
(`NMPIW95` = 95% 구간 폭 ÷ 실제 시계열의 변동 범위). 낮을수록 좁고 예리한 구간이다.

### 2.3 결과

**GAP의 "WMCR 미통과"는 실체가 없는 문제였다.** WMCR 기준으로 GAP은 계속 탈락했는데,
커버리지로 재보니 4개 대상 중 두 번째로 잘 맞는 축에 속했다. 지표의 결함이었지 모형의 결함이 아니었다.

---

## 3. 결정 2 — 커버리지 판정을 "모형 내재 기준분포"로

### 3.1 여기서 크게 헤맸다 (그리고 판정을 뒤집었다)

커버리지를 도입한 직후, NAV의 PICP50이 **0.7405**로 나왔다. 명목수준은 0.50인데 0.74가 나왔으니
"구간이 너무 넓다 = 과대분산"이라고 판정했다. **이 판정은 틀렸다.**

### 3.2 왜 틀렸나

가격 경로는 **자기상관**이 매우 강하다. 오늘 값이 높으면 내일도 높다. 그래서 343일치 관측이 있어도
독립적인 정보는 그보다 훨씬 적다. 경로가 한 번 위로 흐르면 계속 위에 머물고, 아래로 흐르면 계속 아래다.

결과적으로 **경로 하나에서 잰 PICP는 값이 크게 흔들린다.** 모형이 완벽히 옳다고 가정하고
확인해보니 이랬다.

| | 모형이 옳을 때 NAV의 PICP50 |
|---|---|
| 90% 구간 | **0.049 ~ 0.919** |
| 중앙값 | 0.50이 아니라 약 0.55 |
| 평균 | 0.503 (명목과 일치) |

**평균은 명목수준과 정확히 맞는다**(0.503 / 0.793 / 0.944). 지표 자체는 정확하다.
다만 분포가 오른쪽으로 크게 치우쳐서, 경로 하나의 값을 명목수준과 직접 비교하면 안 된다.

### 3.3 어떻게 고쳤나

**사후예측검정** 방식을 도입했다. 절차는 이렇다.

1. 시뮬레이션 경로 하나를 "실제 관측치인 척" 꺼낸다
2. 나머지 경로들로 만든 밴드에 대해 커버리지를 잰다
3. 이를 여러 번 반복해 **"모형이 옳을 때 나올 수 있는 커버리지 값들의 분포"** 를 만든다
4. 진짜 관측치의 커버리지가 이 분포의 몇 백분위인지 본다
5. **5~95 백분위 안이면 모형과 모순되지 않는다**

### 3.4 결과

판정이 완전히 뒤집혔다.

| 대상 | CovErr 날값 | 기준분포 백분위 | 판정 |
|---|---|---|---|
| NAV | 0.1353 | 44.0 | 정상 (양측 p = 0.500) |
| GAP | 0.0379 | **99.0** | **이상** |
| KP | 0.0165 | 15.0 | 정상 |

**NAV는 정상이었고, 오히려 GAP이 문제였다.** 날값만 보면 GAP의 0.0379가 NAV의 0.1353보다
훨씬 좋아 보이지만, 두 계열은 성질이 다르다.

- **NAV는 랜덤워크** → 자기상관이 극심 → 기준분포가 0.049~0.919로 넓게 퍼짐 → 0.74도 평범
- **GAP은 평균회귀** → 관측이 거의 독립 → 기준분포가 0.50 근처로 좁게 모임 → 0.58은 약 3σ

**같은 숫자라도 프로세스의 성질에 따라 의미가 완전히 다르다.** 이것이 이 결정의 핵심이다.

### 3.5 비용 문제도 해결했다

기준분포를 만들려면 경로마다 분위수를 다시 계산해야 해서 300경로에 2분 넘게 걸렸다.
밴드를 전체 경로로 **한 번만** 계산하도록 바꿨다. 경로 하나를 빼고 계산하는 것과의 차이는
1/N 수준이라 무시할 수 있다.

- 느린 방식 중앙값: 0.496 / 0.915 / 1.000
- 빠른 방식 중앙값: 0.497 / 0.915 / 1.000 → **일치**
- 소요 시간: 2분+ → **0.22초** (약 500배)

---

## 4. 결정 3 — 통합 검증(Step 5·6)을 3자 결합으로

### 4.1 무엇이 문제였나

프레임워크의 Step 5는 `NAV × (1+GAP) × (1+KP)` 세 조각을 모두 곱하라고 되어 있고,
Step 6은 그 결과를 검증하라고 되어 있다. 그런데 코드는 **두 조각만** 곱해서 검증했다.

더 심각한 문제가 있었다. 검정에 쓰는 수익률을 이렇게 정의했다.

```
검정 수익률 = ETF / NAV − 1
           = (NAV × (1+GAP)) / NAV − 1
           = GAP        ← 완전히 약분된다
```

즉 **"통합 검증"이라는 이름을 달고 실제로는 GAP 검정을 한 번 더 하고 있었다.**
NAV나 KP를 아무리 바꿔도 이 검정은 반응하지 않았다.

### 4.2 어떻게 고쳤나

- 결합 대상을 세 조각 전부로 바꿨다
- 검정 수익률을 **결합 가격 자체의 로그수익률**로 바꿨다

로그를 쓰면 곱셈이 덧셈이 된다.

```
d log(ETF) = d log(NAV) + d log(1+GAP) + d log(1+KP)
```

이제 세 조각이 모두 검정에 반영된다.

### 4.3 결과

Step 6이 실제 검증이 되었다. PIT-KS p = 0.6543, Kupiec p = 0.6430으로 통과한다.
그리고 **Step 7의 가격 경로 수치는 리팩터 전후로 완전히 동일했다** — 계산을 정리했을 뿐
결과를 바꾸지 않았다는 확인이다.

---

## 5. 결정 4·5 — 보조지표 10종과 모형 비교 코드 삭제

### 5.1 보조지표 10종 삭제

프레임워크는 검증 지표를 **PIT-KS, VaR-Kupiec, ES** 세 가지로만 규정한다.
그런데 코드는 DTW, PMC, RVR, VVS, VPR, VJC, TWAD, KS, 분포적률, VaR/ES근접도까지 10종을 더 냈다.

이 중 셋(`ks`, `var_es`, `distribution_moments`)은 **주검정의 열화판**이었다.
차이는 입력에 있다.

| | 입력 | p-value |
|---|---|---|
| PIT-KS (주검정) | MC 경로 1,000개 전체 | 있음 |
| `ks` (보조) | **대표경로 1개** | 있음 |
| VaR-Kupiec (주검정) | MC 경로 1,000개 전체 | 있음 |
| `var_es` (보조) | **대표경로 1개** | **없음** |

`ks`는 1,000개 경로를 중앙값 하나로 뭉갠 뒤 비교한다. 중앙값 경로는 실제보다 훨씬 매끄러우니
분포가 다를 수밖에 없고, **모형 품질과 무관하게 항상 기각된다**(p ≈ 1e-33). 정보량이 0이다.

`var_es`와 `distribution_moments`는 `1 − |a−b|/|a|` 형태의 "근접도"인데, 이 식은 분모가 작으면
발산한다. 기존에 미결 항목으로 남아 있던 `mean_proximity = −381.8`이 바로 이 병리의 산물이었다.

**삭제 결과**: `metrics.py`가 890줄 → 419줄로 줄고, 미결 항목 4건이 동시에 사라졌다.

### 5.2 모형 비교 코드 삭제

`compare/`, `compare_gap/`, `compare_kp/`, `btc_etf_valid/` 네 패키지는 GBM·Heston·GARCH 등
여러 후보 모형을 비교하는 코드였다. **본 연구는 모형 선택 결론을 다루지 않는다** — 모형은
선행연구와 데이터 특성으로 선정한다. 실제로 프레임워크 11단계 어디에도 대응하는 단계가 없다.

삭제하면서 `compare/metrics.py`만은 살려 `simulator/metrics.py`로 옮겼다. 이 파일은
Step 4·6의 검증 모듈이라 계속 쓰이는데, "모형 비교" 패키지 안에 들어 있던 구조가 원래 어색했다.

**결과**: 4,511줄 제거.

---

## 6. 결정 6 — GAP에 Student-t 혁신항 도입

### 6.1 무엇이 문제였나

결정 2에서 GAP이 이상으로 판정됐다. 원인을 파고들었더니 **분산이 아니라 분포 모양**이었다.

| 측정 | 실제 데이터 | 기존 모형(정규) | 실제의 백분위 |
|---|---|---|---|
| 수준 표준편차 | 0.00417 | 0.00417 | 50.3 (완벽) |
| 변화 표준편차 | 0.00568 | 0.00565 | 55.3 (완벽) |
| **초과첨도** | **+2.147** | **+0.128** | **99.8** |

**초과첨도**는 분포의 꼬리가 정규분포보다 얼마나 두꺼운지를 나타내는 값이다. 0이면 정규분포와
같고, 클수록 "평소엔 조용하다가 가끔 크게 튀는" 성질이 강하다.

실제 ETF 괴리율은 +2.147인데 모형은 +0.128, 즉 **사실상 정규분포**를 만들고 있었다.
변동폭의 크기는 정확히 맞추면서 극단값의 빈도를 재현하지 못한 것이다.

### 6.2 왜 주검정은 이걸 못 잡았나

PIT-KS는 통과했다(p = 0.2631). 모순처럼 보이지만 **검정력 차이**다.

KS 검정은 분포 전체를 보는 만능 검정이라 **꼬리에서 검정력이 낮다**. 첨도 불일치는 정확히
꼬리에서 일어나므로 KS는 놓치고, 그것을 겨냥한 표적 통계량(첨도, 커버리지)은 잡아낸다.

> **"통과"는 "분포가 일치한다"가 아니라 "기각할 증거가 부족하다"이다.**

### 6.3 어떻게 고쳤나

OU 모형의 **혁신항**(매일 가해지는 무작위 충격)을 정규분포에서 **Student-t 분포**로 바꿨다.
t 분포는 정규분포보다 꼬리가 두꺼워 극단값이 더 자주 나온다.

분산이 바뀌지 않도록 표준화해서 넣었다.

```
ε = T_ν / √(ν/(ν−2))     →     평균 0, 분산 1
```

여기서 `ν`(자유도)가 꼬리 두께를 결정한다. 작을수록 꼬리가 두껍고, 무한대면 정규분포가 된다.

**구현에서 한 번 막혔다.** 처음엔 기존 5개 모수와 ν를 한꺼번에 최적화했는데, ν가 초기값 8.0에서
한 발도 움직이지 않았다. 모수들의 크기 차이(κ≈0.9, σ₀≈0.004, ν≈8)가 너무 커서 최적화 알고리즘이
ν 방향의 변화를 감지하지 못한 것이다. **교대 추정**으로 바꿔 해결했다 — (ν 고정 → 나머지 추정)과
(나머지 고정 → ν만 1차원 추정)을 번갈아 반복한다.

### 6.4 검증: 없는 꼬리를 만들어내지는 않는가

새 추정 방식이 제대로 작동하는지 인공 데이터로 확인했다.

| 진짜 ν | 추정된 ν (3회 반복) | 판정 |
|---|---|---|
| 3.0 | 2.8 / 3.71 / 2.83 | 잘 회복 |
| 5.0 | 6.95 / 7.67 / 4.10 | 회복 (다소 흔들림) |
| 10.0 | 13.45 / 32.82 / 7.34 | 회복 (많이 흔들림) |
| **정규분포** | **50.0 / 50.0 / 36.39** | **상한으로 밀림 = 꼬리 없음을 올바르게 판단** |

마지막 줄이 중요하다. **꼬리가 없는 데이터에서는 ν가 상한으로 밀린다.** 즉 이 방법은
없는 꼬리를 지어내지 않는다.

또 하나, **기존 정규 사양으로 돌리면 예전 수치가 그대로 재현된다**(κ = 0.914898,
PIT-KS 0.2631). 리팩터가 기존 결과를 바꾸지 않았다는 확인이다.

### 6.5 결과

실제 데이터에 적합하니 **ν = 5.013**이 나왔다. 상한(50)이 아닌 내부값이므로 데이터가 실제로
두꺼운 꼬리를 지지한다는 뜻이다.

| 지표 | 정규 (기존) | **Student-t (신규)** |
|---|---|---|
| PIT-KS p | 0.2631 | **0.8496** |
| Kupiec p | 0.3494 | **0.4830** |
| PICP50 (백분위) | 0.5802 (**100.0**, 이상) | **0.5131 (65.5, 정상)** |
| CovErr (백분위) | 0.0379 (**99.0**, 이상) | **0.0066 (11.5, 정상)** |

원래 문제였던 첨도가 이제 재현된다.

| 측정 | 실제 | 시뮬 중앙 | 백분위 | 이전 백분위 |
|---|---|---|---|---|
| 초과첨도 (수준) | +2.147 | +2.409 | **42.3** | 99.8 |
| 초과첨도 (변화량) | +1.282 | +1.181 | **55.0** | 99.0 |
| 수준 표준편차 | 0.00417 | 0.00417 | 50.9 | 50.3 |

**꼬리만 고치고 분산 적합은 그대로 유지됐다**(50.9 백분위).

---

## 7. 결정 7 — KP를 3레짐에서 2레짐으로

### 7.1 무엇이 문제였나

김치프리미엄(KP) 모형은 상황에 따라 다른 동학을 쓰는 **Threshold-OU**다. 원래 세 국면이었다.

| 레짐 | 조건 | 뜻 |
|---|---|---|
| 0 | \|KP\| ≤ τ | 평상시 |
| 1 | KP > τ | 프리미엄이 큰 국면 |
| 2 | KP < −τ | **역프리미엄 국면** |

문제는 **레짐 2가 한 번도 관측되지 않았다**는 점이다. 표본 기간(2024-01-12 ~ 2025-05-23) 중
김치프리미엄의 최저치가 −2.57%인데 −τ는 −4.30%라, 그 아래로 내려간 날이 없었다.

관측이 없으니 모수를 추정할 수 없고, 코드는 **폴백 기본값**을 채워 넣었다. 그런데 그 기본값이
하필 최악의 조합이었다.

| | 레짐0 | 레짐1 | **레짐2 (기본값)** |
|---|---|---|---|
| 점유율 | 79.6% | 20.4% | **0.005%** |
| kappa (회귀 속도) | 0.3230 | 0.3237 | **0.1000** ← 3배 느림 |
| sigma0 (변동성) | 0.01001 | 0.01269 | **0.02463** ← 2.5배 큼 |

**변동성은 가장 크고 되돌아오는 속도는 가장 느리다.** 한번 들어가면 잘 빠져나오지 못하는
"함정" 레짐이 되어버린 것이다.

Base 시뮬레이션에서는 1,000경로 중 7개가 중앙값 1일 머무는 수준이라 영향이 없었다. 하지만
**Phase 2의 위기 시나리오는 KP 변동성을 키우므로 진입 빈도가 올라간다.** 그러면 추정되지 않은
모수가 결과를 밀게 된다.

### 7.2 어떻게 고쳤나

두 국면으로 축소했다: `KP ≤ τ` / `KP > τ`. 임계값 하나짜리 표준 SETAR 형태다.

**축소해도 KP가 음수로 못 가는 것은 아니다.** 별도 모수셋을 쓰지 않고 레짐 0의 동학을 따를 뿐이다.
표현력 손실은 생각보다 작고, 데이터에 근거 없는 모수를 두는 것보다 정직하다.

임계값 선택에 쓰는 BIC의 모수 개수도 `3×3 + 3×3` 하드코딩에서 레짐 수에 연동되도록 고쳤다.

### 7.3 결과: 아무것도 바뀌지 않았다 (그리고 그게 맞다)

Base를 재실행한 결과 **20개 지표가 소수 4자리까지 전부 동일**했다.

문제는 "정말 반영된 것"과 "변경이 안 먹은 것"을 구분해야 한다는 점이다. 경로 수준에서 확인했다.

| 확인 | 결과 |
|---|---|
| 완전히 동일한 경로 | **993 / 1,000** |
| 차이 나는 경로 | 7개 |
| 그 7개가 3레짐일 때 레짐2에 진입했던 경로와 일치하는가 | **정확히 일치 (집합 동일)** |
| 차이 나는 경로의 최대 차이 | **5.59e-02** (KP 5.6%p) |

즉 변경은 확실히 반영됐고, **영향받은 경로는 정확히 함정 레짐에 빠졌던 것들뿐**이다.
그 경로들에서 KP가 5.6%p나 벌어졌다는 것은, 함정 레짐이 실제로 경로를 왜곡하고 있었음을 보여준다.

---

## 8. Phase 1 최종 결과

위 결정들을 모두 반영한 최종 상태다.

### 8.1 모형 사양

**NAV — ARIMAX(1,0,1)-GARCH(1,1)-t**

| 모수 | 값 | 의미 |
|---|---|---|
| AR(1) | −0.4616 | 어제 수익률의 영향 |
| MA(1) | +0.3905 | 어제 충격의 잔여 영향 |
| omega | 4.0519 | 변동성의 기저 수준 |
| alpha1 | 0.0739 | 어제 충격이 오늘 변동성에 미치는 영향 |
| beta1 | 0.6031 | 변동성의 지속성 |
| nu | 4.064 | 꼬리 두께 (작을수록 두꺼움) |

외생변수는 Hash Rate와 Unique Addresses인데 **기여도가 매우 작다**. 1 표준편차 변화가
Log Return에 주는 영향이 각각 +0.000078, +0.001697로, 일별 수익률 표준편차 0.03408의
0.2%와 5% 수준이다.

**GAP — OU + 외생변수 연동 + Student-t 혁신항**

| 모수 | 값 |
|---|---|
| kappa (회귀 속도) | 0.9354 |
| mu (장기 평균) | 0.000597 |
| sigma0 (기저 변동성) | 0.004175 |
| delta1 (검색량 연동) | +0.10505 |
| delta2 (BTC 변동성 연동) | −0.03933 |
| **nu (자유도)** | **5.013** |

**KP — Threshold-OU 2레짐**

| 레짐 | 조건 | 점유율 | kappa | mu | sigma0 |
|---|---|---|---|---|---|
| 0 | KP ≤ τ | 79.6% | 0.3230 | 0.01514 | 0.01001 |
| 1 | KP > τ | 20.4% | 0.3237 | 0.06353 | 0.01269 |

임계값 τ = 0.042976.

### 8.2 검증 결과 — 4개 전부 통과

**주검정** (프레임워크가 규정한 세 가지)

| 대상 | PIT-KS p | Kupiec p | VaR 초과율 | ES tail_error | 판정 |
|---|---|---|---|---|---|
| NAV | 0.9794 | 0.6521 | 5.54% | +0.00334 | 통과 |
| GAP | 0.8496 | 0.4830 | 5.85% | +0.00039 | 통과 |
| KP | 0.9040 | 0.2434 | 6.43% | −0.00284 | 통과 |
| 통합 (Step 6) | 0.6543 | 0.6430 | 5.56% | −0.00196 | 통과 |

세 검정을 쉽게 풀면 이렇다.

- **PIT-KS**: 실제 값이 시뮬레이션 분포 안에서 골고루 흩어져 있는가 (한쪽으로 쏠리면 기각)
- **VaR-Kupiec**: 5%라고 예측한 손실 한계를 실제로 5% 정도만 넘었는가
- **ES**: 그 한계를 넘었을 때의 평균 손실 크기가 맞는가

명목 VaR 수준이 5%인데 네 대상 모두 초과율이 5~6.5%로 근처에 있다.

**보조 진단** (예측구간 커버리지, 판정은 기준분포 백분위로)

| 대상 | PICP50 | 백분위 | CovErr | 백분위 | 판정 |
|---|---|---|---|---|---|
| NAV | 0.7405 | 73.0 | 0.1353 | 44.0 | 정상 |
| GAP | 0.5131 | 65.5 | 0.0066 | 11.5 | 정상 |
| KP | 0.4665 | 33.0 | 0.0165 | 15.0 | 정상 |
| 통합 | 0.7493 | 75.5 | 0.1227 | 35.0 | 정상 |

**4개 모두 5~95 백분위 안**이다. 작업 시작 시점에는 GAP이 99~100 백분위로 이상이었다.

### 8.3 Step 7 — 가격 경로

기간 2024-01-12 ~ 2025-05-23 (343 거래일), 초기값 10,000원 앵커링.

| | 최종값 |
|---|---|
| 실제 대리 계열 | 22,762.67원 |
| 시뮬 중앙값 | 16,657.74원 |
| 시뮬 하위 5% | 6,022.04원 |
| 시뮬 상위 95% | 52,572.70원 |

한국형 ETF는 실재하지 않으므로, "실제"는 실제 NAV·GAP·KP로 구성한 **대리 시계열**이다.

실제값(22,763원)이 시뮬 중앙값(16,658원)보다 높지만 5~95% 구간 안에 넉넉히 들어간다.
343일을 복리로 누적하면 분산이 √T로 커지므로 이 정도 폭은 모형상 정상이며, 커버리지 검정이
이를 정량적으로 확인해준다.

### 8.4 참고 — 모형 선정의 데이터 특성 근거

모형은 선행연구와 데이터 특성으로 선정한다. 그중 데이터 특성 쪽을 수치로 확인한 결과다
(`analysis/data_characteristics.py`, 본 파이프라인과 독립 실행).

| 컴포넌트 | 요구되는 성질 | 검정 | 결과 | 지지 여부 |
|---|---|---|---|---|
| NAV | 두꺼운 꼬리 → **t** | Jarque-Bera | 초과첨도 +2.086, p < 0.001 | **지지** |
| NAV | 자기상관 → **ARMA** | Ljung-Box(10) | p = 0.321 | 지지 안 됨 |
| NAV | 변동성 군집 → **GARCH** | ARCH-LM·LB(r²)·LB(\|r\|) | 9개 조합 전부 p = 0.24~0.91 | 지지 안 됨 |
| GAP | 평균회귀 → **OU** | ADF + KPSS | ADF p < 0.001, KPSS p = 0.100 | **지지** |
| KP | 평균회귀 → OU | ADF (사양별) + KPSS | ADF 0.157 / 0.003 / 0.041 | 사양에 민감 |
| KP | 임계효과 → **Threshold** | 블록순열 (B = 500) | p = 0.396 | 지지 안 됨 |

**이것은 "모형이 틀렸다"는 뜻이 아니다.** 이 343일 표본이 해당 성질을 독립적으로 확인해주지
못한다는 뜻이다. 암호자산의 변동성 군집은 선행연구에서 확립된 사실이고 T = 343은 짧아,
검정력 문제일 가능성이 크다. 단일 검정의 우연이 아님을 확인하려고 ARCH 효과는
세 가지 검정 × 세 가지 래그로 아홉 번 봤는데 전부 기각되지 않았다.

한편 **GAP 변화량의 초과첨도 +1.282 (p < 0.001)** 는 6장의 커버리지 검정이 잡아낸 신호와
같은 것이다. 서로 독립적인 두 경로가 같은 결론을 가리키므로, GAP의 두꺼운 꼬리는 실재하는
특성으로 볼 수 있다. 이것이 Student-t 도입을 뒷받침한다.

---

## 9. Phase 2 결과

### 9.1 시나리오 정의

5개 외생변수의 레짐 조합으로 9개 시나리오를 정의했다.

| ID | Bitcoin_RV | VKOSPI | KR_Volume | KR_SVI | Global_SVI | 공동빈도 |
|---|---|---|---|---|---|---|
| S01 | Low | Normal | Low | Low | Mid | 67 |
| S02 | Mid | Normal | High | High | High | 45 |
| S03 | Low | Normal | Low | Low | Low | 23 |
| S04 | Mid | Normal | Low | Low | Low | 22 |
| **S05** | **High** | **Extreme** | **High** | **High** | **High** | 21 |
| S06 | Mid | Normal | High | High | Mid | 17 |
| S07 | Mid | Normal | Low | Low | Mid | 11 |
| S08 | Mid | Normal | High | High | Low | 0 |
| S09 | High | Extreme | High | High | Mid | 0 |

S05가 위기 시나리오다 — 비트코인 변동성 최고, 코스피 변동성 극단, 거래량과 관심도 급증이
동시에 일어나는 상황이다. S08·S09는 이론적으로만 정의되고 표본 기간 중 관측되지 않았다.

### 9.2 시나리오별 검증 (Step 9·10)

9개 전부 새 사양(GAP ν = 5.013, KP 2레짐)으로 재실행했다.

| ID | GAP PIT-KS | GAP CovErr (백분위) | KP PIT-KS | KP CovErr (백분위) |
|---|---|---|---|---|
| S01 | 0.7009 | 0.0245 (86.0) | 0.4442 | 0.0216 (29.5) |
| S02 | 0.0686 | 0.0634 (100.0) | 0.0652 | 0.0411 (65.0) |
| S03 | 0.9312 | 0.0056 (8.0) | 0.4442 | 0.0216 (29.5) |
| S04 | 0.8804 | 0.0172 (62.5) | 0.4442 | 0.0216 (29.5) |
| S05 | 0.2156 | 0.0381 (99.0) | 0.0652 | 0.0411 (65.0) |
| S06 | 0.8915 | 0.0087 (20.5) | 0.0652 | 0.0411 (65.0) |
| S07 | 0.8915 | 0.0087 (20.5) | 0.4442 | 0.0216 (29.5) |
| S08 | 0.8804 | 0.0172 (62.5) | 0.0652 | 0.0411 (65.0) |
| S09 | 0.9251 | 0.0153 (53.5) | 0.0652 | 0.0411 (65.0) |

**9개 모두 PIT-KS를 통과한다.** 다만 S02와 S05의 GAP 커버리지가 99~100 백분위로 높다.
둘 다 외생변수가 High/Extreme인 시나리오라, 외생변수를 극단값으로 교체했을 때 GAP 모형이
다소 어긋난다는 신호로 볼 수 있다.

### 9.3 시나리오별 리스크 지표

9개 시나리오와 Base의 최종 가격 분포에서 산출한 위험 지표다 (단위: 원, 초기값 10,000원 기준).

| 시나리오 | VaR_95 | CVaR_95 | VaR_99 | CVaR_99 | Volatility | p50 | p95 |
|---|---|---|---|---|---|---|---|
| Base | 6,022 | 4,678 | 3,824 | 3,498 | 572.4 | 16,658 | 52,573 |
| S01 | 5,965 | 4,694 | 3,815 | 3,517 | 583.7 | 16,696 | 52,459 |
| S02 | 5,958 | 4,692 | 3,816 | 3,522 | **594.2** | 16,730 | 52,390 |
| S03 | 5,965 | 4,694 | 3,814 | 3,517 | 582.6 | 16,691 | 52,460 |
| S04 | 5,965 | 4,694 | 3,814 | 3,518 | 582.0 | 16,694 | 52,462 |
| **S05** | 5,958 | 4,692 | 3,815 | 3,523 | **592.9** | 16,722 | 52,392 |
| S06 | 5,957 | 4,692 | 3,814 | 3,524 | 591.0 | 16,707 | 52,395 |
| S07 | 5,965 | 4,694 | 3,815 | 3,517 | 583.0 | 16,691 | 52,460 |
| S08 | 5,957 | 4,692 | 3,814 | 3,525 | 590.0 | 16,710 | 52,397 |
| S09 | 5,957 | 4,692 | 3,814 | 3,525 | 590.1 | 16,708 | 52,397 |

지표를 쉽게 풀면 이렇다.

- **VaR_95**: 하위 5%에 해당하는 가격. "나쁜 경우 이 정도까지 떨어진다"
- **CVaR_95**: 그 하위 5% 구간의 평균 가격. VaR보다 더 나쁜 상황들의 평균
- **Volatility**: 가격 경로의 변동성
- **p50 / p95**: 중앙값 / 상위 5% 가격

눈으로 보면 차이가 작아 보인다. **변동성이 가장 크게 갈린다** — 평온한 S03·S04는 582 근처,
관심도와 거래량이 높은 S02·S05는 593~594다. 이 차이가 통계적으로 유의한지는 다음 절에서 다룬다.

> **주의 — 시나리오 모수는 재추정되지 않는다.**
> 프레임워크 Step 10은 "외생변수 교체 + 모수 재추정"이라고 표기하고 코드의 설명문도 그렇게
> 적혀 있으나, **실제 구현은 Base 모수를 고정하고 외생변수만 교체한다.** 실제로 비교표에서
> 9개 시나리오의 `gap_kappa`가 0.935399로 모두 동일하다.
>
> 이 설계에서 시나리오 효과는 오직 변동성 식을 통해서만 들어온다:
> `σ_t = σ_0 · exp(δ1·검색량 + δ2·변동성)`. 외생변수가 High/Extreme이면 σ_t가 커지는 방식이다.
> 모수를 고정하면 시나리오 간 차이가 외생변수 채널로만 오게 되어 해석이 깔끔해지는 장점이 있으나,
> **프레임워크 표기와 코드 설명문이 실제 동작과 어긋나 있다.** 둘 중 하나로 맞춰야 한다
> (11장 참조).

### 9.4 유의성 검정 (Step 11)

**무엇을 검정하나.** 시나리오별 리스크 지표가 Base와 다르게 나왔을 때, 그 차이가 진짜인지
아니면 몬테카를로의 우연인지 판단한다.

**방법.** Base와 각 시나리오를 **100번 독립 반복** × 1,000경로로 돌린다. 반복마다 Base와
시나리오에 **같은 난수**를 쓴다(공통난수, CRN). 그러면 두 결과의 차이는 오직 외생변수에서만
오게 되어, 난수 때문에 생기는 잡음이 상쇄된다. 반복별 차이를 모아 대응표본 검정을 한다.

**판정 기준이 두 개인 이유.** 100번 반복에 공통난수를 쓰면 표준오차가 매우 작아져서
**0.5% 수준의 차이도 통계적으로 유의**해진다. p-value만으로는 변별이 안 된다.
그래서 **유의성과 효과크기를 함께** 본다.

> `priority` = p < 0.05 **그리고** |효과크기| ≥ 0.25%
> 그 외는 `noise`

**결과**

| 지표 | 효과크기 (중앙값) | 유의한 시나리오 | 판정 |
|---|---|---|---|
| **Volatility** (변동성) | **3.012%** | 9 / 9 | priority |
| p50 (중앙값 가격) | 0.924% | 9 / 9 | priority |
| VaR_99 | 0.799% | 9 / 9 | priority |
| Max_DD_mean (최대낙폭) | 0.694% | 9 / 9 | priority |
| CVaR_95 | 0.535% | 9 / 9 | priority |
| CVaR_99 | 0.512% | 9 / 9 | priority |
| VaR_95 | 0.223% | 9 / 9 | noise |
| p95 (상위 5% 가격) | 0.126% | **1 / 9** | noise |

**해석**

1. **시나리오 차이는 변동성에서 가장 뚜렷하다.** 3.012%로 다른 지표의 3배 이상이다.
   레짐이 바뀌면 가격의 흔들림 자체가 가장 크게 반응한다는 뜻이다.

2. **중앙값·꼬리 평균·최대낙폭·VaR_99에서도 일관되게 나타난다** (0.5~0.9%).
   9개 시나리오 전부 같은 방향이라 지표별 판정이 시나리오에 따라 흔들리지 않는다.

3. **분포의 양 끝단에서는 실질적 차이가 없다.** VaR_95는 유의하지만 효과크기가 0.223%로
   임계값 아래고, p95는 아예 9개 중 1개에서만 유의하다. 즉 아주 좋은 경우(상위 5%)와
   가벼운 손실 구간(VaR_95)은 시나리오가 바뀌어도 거의 그대로다.

**위기 시나리오는 더 크게 반응한다.** 평온한 S01과 위기 S05를 비교하면 이렇다.

| 지표 | S01 (평온) | S05 (위기) | 배율 |
|---|---|---|---|
| Volatility | +1.874% | **+3.518%** | 1.9배 |
| Max_DD_mean | −0.312% | **−0.755%** | 2.4배 |
| p50 | +0.718% | +0.927% | 1.3배 |
| VaR_99 | +0.652% | +0.803% | 1.2배 |
| VaR_95 | +0.217% | +0.214% | 1.0배 |
| p95 | +0.163% | +0.117% | 0.7배 |

**변동성(1.9배)과 최대낙폭(2.4배)이 위기 시나리오에서 크게 벌어진다.** 반면 VaR_95와 p95는
평온·위기가 거의 같다. 이는 위 판정과 일치한다 — 시나리오가 영향을 주는 곳은 분포의 중심부와
극단 손실 쪽이지, 분포의 양 끝단이 아니다.

**다중비교 보정은 적용하지 않았다.** 사전에 설계된 시나리오이므로 "모든 시나리오가 Base와
같다"는 만능귀무가설을 세우는 것이 부적절하기 때문이다(Rothman 1990).

---

## 10. 작업 중 발견한 문제들

결정을 실행하는 과정에서 코드 결함 몇 가지가 드러났다. 기록해둔다.

### 10.1 `--no-cache`가 저장까지 막고 있었다

KP를 2레짐으로 바꾼 뒤 Phase 2를 돌렸더니, 시나리오 산출물에 `n_regimes=3`이 실려 나왔다.

원인은 `--no-cache` 옵션이었다. 이 옵션은 "저장된 결과를 읽지 않는다"는 뜻이어야 하는데,
**저장도 하지 않았다.** 그래서 `sim_meta.json`이 전날 파일로 남았고, 이를 Base 모수의 출처로
삼는 Phase 2가 옛 사양을 읽었다. GAP의 `nu`는 전날 t 실행 때 이미 들어가 있어서,
**GAP은 새 사양 / KP는 옛 사양**인 혼합 상태로 시나리오 5개가 생성됐다. 폐기하고 다시 돌렸다.

저장 조건을 `no_cache`에서 `no_save`로 바꿔, 강제 재실행 후에도 결과가 저장되도록 고쳤다.

### 10.2 비교표가 레짐 3개를 하드코딩하고 있었다

Step 9·10의 마지막 단계인 비교표 생성이 `KeyError: '2'`로 죽었다. 시나리오 9개 산출물은
정상 저장됐지만 요약 CSV를 못 만들었다. 존재하는 레짐만 담도록 고쳤다.

덕분에 요약 CSV 두 개가 **2026년 6월 파일 그대로**였다는 것도 드러났다 — 이미 제거한 `wmcr`
컬럼과 3레짐 `kappa_r2`가 남아 있었다.

### 10.3 판정을 두 번 뒤집었다

이 작업에서 두 번 잘못된 판정을 내렸고, 둘 다 정정했다.

1. **"NAV 과대분산"** → 오판. PICP를 명목수준과 직접 비교한 것이 원인. 기준분포로 재판정하니 정상(p = 0.500).
2. **"GAP 분포 모양이 어긋난다"** → 과한 단정. 주검정은 통과했고 표적 통계량이 신호를 줄 뿐이다.

교훈은 코드에 반영했다. 이제 `simulator/main.py`가 커버리지를 낼 때 **기준분포 백분위와
[정상]/[이상] 판정을 함께 출력**하므로, 같은 오독을 반복하기 어렵다.

---

## 11. 재현 방법

```bash
# Phase 1 — Base 시뮬레이션 및 검증
python simulator/main.py

# 주요 옵션
python simulator/main.py --gap-dist normal   # GAP 혁신항을 정규분포로 (기존 사양)
python simulator/main.py --kp-regimes 3      # KP를 3레짐으로 (기존 사양)
python simulator/main.py --no-cache          # 캐시를 읽지 않고 강제 재실행

# Phase 2 — 시나리오 시뮬레이션 및 유의성 검정
echo all | python simulator/scenario_main.py
echo all | python simulator/results_main.py

# Step 3 모형 선정의 데이터 특성 근거 (선택)
python analysis/data_characteristics.py
```

기본값이 최종 사양이다: GAP은 Student-t, KP는 2레짐.

---

## 12. 남은 과제

우선순위 순이다.

### 12.1 Step 10의 "모수 재추정" 불일치 — 결정 필요

프레임워크 그림의 Step 10은 **"GAP/KP 재추정 및 시뮬레이션 — 외생변수만 교체, 모수 재추정"**
이라고 되어 있고 `scenario_main.py`의 설명문도 "시나리오별 GAP / KP 모수 재추정"이라고 적혀 있다.
그러나 **실제 코드는 Base 모수를 고정한다** (`[GAP] Base 모수 고정`). 9개 시나리오의
`gap_kappa`가 전부 0.935399로 동일한 것이 그 증거다.

커밋 이력을 보면 과거에 두 갈래가 있었다.

| 커밋 | 방식 |
|---|---|
| `f14c00a` | 시나리오별 모수 재추정 |
| `df14167` | 모수 고정, 외생변수만 교체 ← **현재 코드** |

둘 중 어느 쪽이 최종본인지 정하고, 프레임워크 그림·코드 설명문·실제 동작을 일치시켜야 한다.
**재추정으로 바꾸면 Phase 2 전체를 다시 돌려야 한다**(약 40분).

- **고정을 택하면**: 시나리오 효과가 외생변수 채널로만 들어와 해석이 깔끔하다.
  그림과 설명문을 "모수 고정"으로 고치면 된다. 재실행 불필요.
- **재추정을 택하면**: 그림대로가 되지만 Phase 2 전체 재실행이 필요하고,
  시나리오별로 모수가 달라져 차이의 원인이 외생변수인지 모수인지 분리하기 어려워진다.

### 12.2 S02·S05의 GAP 커버리지

두 시나리오에서 GAP의 CovErr 백분위가 99~100으로 높다(9.2절). 둘 다 외생변수가
High/Extreme인 시나리오다. 외생변수를 극단값으로 교체했을 때 GAP 모형이 다소 어긋난다는
신호일 수 있다. Base에서는 11.5 백분위로 정상이므로 모형 자체의 문제는 아니다.

### 12.3 데이터 특성 근거의 취급

`analysis/data_characteristics.py` 결과를 논문에 넣을지 결정해야 한다. 이 표본(T=343)에서는
**GARCH 효과와 Threshold 효과가 통계적으로 지지되지 않는다**(8.4절 참조). 선행연구를 주 근거로
세우고 데이터 특성은 보조로 두는 편이 안전하다.

### 12.4 문서·저장소 정리

- `RESEARCH_SUMMARY.md`를 프레임워크 11단계 구조로 재편
- `etf/` 가상환경과 `__pycache__` 추적 제외
- `risk_metrics_comparison.csv`가 2026-06-26 파일로 남아 있음 (다른 산출물은 모두 갱신됨)
